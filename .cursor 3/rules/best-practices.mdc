# Best Practices

- 모든 함수는 명확한 입력/출력을 가지며 side effect를 최소화할 것.
- `console.log`는 개발 중에만 허용하며, PR에는 포함하지 않는다.
- Exception은 반드시 명확한 에러 타입으로 throw하고, try-catch는 최소한으로만 사용한다.
- 파일 하나당 하나의 주된 역할만 담당하도록 구성할 것.
- 불필요한 주석보다는 코드 자체의 가독성을 우선한다.
- 각 레이어 간 의존성을 명확히 하며, `domain → service → adapter` 방향만 허용한다.
